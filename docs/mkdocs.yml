site_name: titanic
# 
site_author: 3ndetz
# 
plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [..]
          options:
            show_root_heading: true
            show_source: true
