# HW 6 Fix

Комментарий: нет отчетов об экспериментах (должны выгружаться в io)

## Эксперименты: текущий статус

`dvc exp run train --name hw6f_nn_4_bad -S train.pipeline=neural_network -S train.test_size=0.5`

`dvc exp run train --name hw6f_nn_4 -S train.pipeline=neural_network -S train.test_size=0.1`